#include <iostream>
using namespace std;

int main() {
    int a = 10;
    int b=15;
    char c='A';
    // so this is the comment part which is ignore by the complier
    // ctrl+/ is the shortcut for commenting the line
     cout<<((a<b)) || ((a==b));
     cout<<endl;
    return 0;
}
